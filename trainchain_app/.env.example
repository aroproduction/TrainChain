# TrainChain contributor app — environment configuration
# Copy this file to .env and edit as needed.
# In production the packaged exe reads this file from the same directory.

# Backend API base URL (no trailing slash)
API_URL=https://trainchain.onrender.com

# Uncomment and set this for local development:
# API_URL=http://localhost:3000
